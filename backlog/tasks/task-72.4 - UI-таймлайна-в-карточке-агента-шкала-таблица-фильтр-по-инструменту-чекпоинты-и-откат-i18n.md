---
id: TASK-72.4
title: >-
  UI таймлайна в карточке агента: шкала, таблица, фильтр по инструменту,
  чекпоинты и откат, i18n
status: Review
assignee: []
created_date: '2026-09-19 11:12'
updated_date: '2026-09-19 11:47'
labels:
  - swarm
  - ui
dependencies:
  - TASK-72.2
  - TASK-72.3
parent_task_id: TASK-72
priority: medium
type: feature
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
decision-45. Вкладка «Таймлайн» в карточке агента арены: сводка, горизонтальная шкала запусков/ходов/инструментов, таблица с длительностью, статусом, размером вывода, токенами и стоимостью хода, HITL; фильтр по инструменту; список чекпоинтов с кнопкой отката (DialogHost), «Продолжить агента». Строки ru/en, чистый помощник раскладки в src/lib.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Вкладка показывает шкалу и таблицу по реальной сессии, фильтр по инструменту работает
- [x] #2 Откат и продолжение доступны из вкладки с подтверждением DialogHost
- [x] #3 Все строки через i18n ru/en, помощник раскладки покрыт тестами
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
decision-45.
- `src/components/ai/swarm/AgentTimelinePanel.tsx` — вкладка «Таймлайн»/«Timeline» карточки агента арены: сводка (запуски, ходы, инструменты, ошибки, время, время в инструментах, стоимость с `~` при оценке), обновить, «Трасса .jsonl», «Продолжить агента»; фильтр по инструменту; шкала — строка на запуск, окно своего запуска, ходы фоном с чередованием и номером, инструменты цветом статуса; таблица с разделителями запусков: ход (модель, длительность, токены, стоимость; частичный выход Claude CLI — прочерк с подсказкой), инструмент (аргументы, длительность, статус, размер вывода, HITL); переключения модели, ошибки; чекпоинты новыми сверху с «Откатить сюда» (недоступно — с причиной), история откатов. Во время работы агента трасса перечитывается не чаще раза в 2 с.
- `SwarmArenaView`: вкладка, бейдж «откат к #N», кнопка «.jsonl» (трасса всех агентов) в шапке сессии.
- `src/lib/agentTimelineView.ts` (чистый): фильтр, строки таблицы, раскладка шкалы, форматирование (единицы из словаря), подписи чекпоинтов; тесты `agentTimelineView.test.ts` (5).
- i18n: секция `agentTimeline` в types/ru/en, включая статусы запуска и единицы времени.
- Скриншоты собранного exe (копия userData без кэшей, аудита, HITL, логов, remote/computer-use; временный HOME; сессия записана настоящим прогоном `AgentFleetService` с Claude CLI haiku, `projectPath` в копии переписан на зарегистрированный F:\ProjectHub, worktree — в scratchpad): EN и RU, шкала, фильтр Write (2 строки), подтверждение отката в DialogHost (отменено). По скриншотам исправлено: общая шкала на все запуски → окно каждого запуска; частичный выход «~2» → прочерк; разделители запусков; «1 files removed» → «files removed: 1»; непереведённые статус запуска и единицы «s/ms».
<!-- SECTION:NOTES:END -->
