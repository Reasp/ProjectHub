---
id: TASK-80
title: >-
  Планировщик: роль architect → план → подзадачи в Backlog с зависимостями →
  граф → авто-диспетчер в слоты (worktree на подзадачу)
status: In Progress
assignee: []
created_date: '2026-09-15 03:14'
updated_date: '2026-09-20 02:31'
labels:
  - swarm
  - planning
  - backlog
  - orchestration
milestone: m-0
dependencies:
  - TASK-75
references:
  - >-
    backlog/decisions/decision-49 -
    Планировщик-подзадачи-Backlog-как-план-интеграционная-ветка-и-диспетчер-по-готовности-узлов.md
priority: medium
type: feature
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Devin/Jules продают «fire-and-forget» выполнение больших задач; Claude Code — plan mode и параллельные субагенты; Codex Ultra — «maximum reasoning with automatic task delegation». В ProjectHub есть режимы `fan_out` (все делают одно) и `handoff` (конвейер), но нет декомпозиции: большую задачу нельзя разбить на подзадачи с зависимостями и раздать нескольким агентам параллельно. Backlog.md уже поддерживает `parentTaskId` и `dependencies` — их и используем как модель плана (doc-10).

## Контур
1. Роль `architect` получает задачу + контекст и возвращает структурированный план: список подзадач (title, description, AC, dependencies, оценка размера, рекомендованный тир модели), зафиксированный как `implementationPlan` родительской задачи и как реальные подзадачи в `backlog/tasks/` (через `backlogTaskCreate`, `parentTaskId`, `dependencies`).
2. Человек правит план в UI (граф зависимостей, перетаскивание, слияние/разделение узлов) и подтверждает — без подтверждения ничего не запускается.
3. Диспетчер: подзадачи без незакрытых зависимостей запускаются параллельно в режиме «до готовности» (каждая — свой worktree/ветка от базовой), с лимитом параллельности и общим бюджетом; при завершении подзадачи её ветка сливается в интеграционную ветку родителя (decision-8), после чего запускаются зависимые.
4. Конфликты слияния — HITL-запрос с диффом; провал подзадачи — остановка зависимых и уведомление.
5. По завершении всех — родительская задача → Review с сводкой (подзадачи, стоимость, чеки).

## Что сделать
- `planSchema.ts` (zod, валидация плана, тесты на циклы зависимостей/топологическую сортировку) + `planDispatcher.ts` (чистая логика готовности узлов, тесты) + `planService.ts`.
- Промпт роли architect для структурированного плана в `contextBuilder`.
- UI: вкладка «План» в карточке задачи — граф (DAG) с состояниями узлов, кнопки «Сгенерировать план», «Утвердить и запустить», «Остановить всё».
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Схема плана и диспетчер — чистые модули с тестами (валидация, циклы, топологическая сортировка, готовность узлов)
- [x] #2 Роль architect создаёт подзадачи в backlog/tasks с parentTaskId и dependencies в нативном формате Backlog.md (TASK-31)
- [x] #3 План отображается как граф с возможностью правки; запуск только после явного утверждения человеком
- [ ] #4 Независимые подзадачи выполняются параллельно в своих worktree в режиме «до готовности» с лимитом параллельности и общим бюджетом
- [x] #5 Завершённые подзадачи сливаются в интеграционную ветку; конфликты и провалы идут в HITL/уведомления, зависимые не стартуют
- [ ] #6 Родительская задача получает сводку и переводится в Review; ADR о модели плана; i18n; lint/test зелёные, pack:win собран
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Решение — decision-49 (proposed), подзадачи 80.1–80.4.

Сделано в этой сессии (80.1–80.3 в Review):
- Чистые модули `planSchema.ts` (ограда ```projecthub-plan```, zod, нормализация, циклы, топологическая сортировка) и `planDispatcher.ts` (готовность узла по `merged`, лимит параллельности, бюджет, блокировка зависимых и недостижимых, исход, хэш графа) + `planTypes.ts`. 45 unit-тестов.
- `planService.ts` и `planStore.ts`: ход роли `architect` с повтором при невалидном плане, создание подзадач Backlog.md (`parent_task_id`, `dependencies`, критерии), пересчёт графа из файлов задач, интеграционная ветка `plan/<taskId>` со своим worktree, запуск узлов через `startDoneLoop` от неё, слияние внутри интеграционного worktree, конфликт → HITL, сводка и Review у родителя. IPC, preload, типы, инициализация в `main.ts`. 9 тестов на настоящем `AgentFleetService`.
- UI: `PlanPanel` в карточке задачи (граф по шагам, состояния узлов, генерация, утверждение, повторное подтверждение изменённого графа, остановка, конфликт, пропуск узла), чистый `planGraphView.ts` с 14 тестами, строки ru/en.
- Попутно исправлено: `taskIdToFilePrefix`/`findTaskFile` путали подзадачу `TASK-80.1` с родителем; `detectBaseBranch` игнорировал явно переданную базовую ветку.
- Живая проверка роли `architect` на qwen2.5:7b-instruct (локальная Ollama): валидный план с первой попытки, подзадачи созданы; инструкция поправлена после того, как модель скопировала критерии из примера.

Осталось (TASK-80.4): живой прогон самих узлов с HITL через MCP (AC #4 подтверждён пока тестами на настоящем флоте, но не живой моделью) и скриншоты вкладки «План» в собранном exe.
<!-- SECTION:NOTES:END -->
