---
id: TASK-80.3
title: >-
  UI плана в карточке задачи: граф узлов, генерация, утверждение и остановка,
  i18n
status: Done
assignee: []
created_date: '2026-09-20 01:59'
updated_date: '2026-09-21 02:08'
labels:
  - swarm
  - planning
  - ui
dependencies:
  - TASK-80.2
parent_task_id: TASK-80
priority: medium
type: feature
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
decision-49 п. 5–6. Вкладка «План» в `TaskDetailModal`: граф DAG с состояниями узлов (ожидает, работает, слит, конфликт, провал, заблокирован, пропущен), кнопки «Сгенерировать план», «Утвердить и запустить», «Остановить всё», пометка узла `skipped` до старта, баннер «план изменился — подтвердить», переход к сессии узла, стоимость и бюджет плана, карточка конфликта слияния. Модалки — через `createPortal`, подтверждения — `DialogHost` z-[10001] (decision-17). Строки ru/en.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Граф плана показывает узлы, зависимости и состояния, обновляется по ходу работы
- [x] #2 Кнопки генерации, утверждения и остановки работают; без утверждения узлы не стартуют
- [x] #3 Конфликт слияния виден в UI с файлами и действиями человека
- [x] #4 Тексты ru/en, модалки через createPortal, подтверждения через DialogHost
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
decision-49 п. 5–6.

- `src/lib/planGraphView.ts` (чистый, без React): `planLevels` — раскладка графа по уровням (узлы одного уровня идут параллельно; цикл не зацикливает раскладку, зависимость вне плана не влияет), `nodeTone`, `planActions` (что доступно: генерация, утверждение, повторное подтверждение изменённого графа, остановка, удаление), `planCounts`, `formatPlanCost`. Тесты `planGraphView.test.ts` (14).
- `src/components/kanban/PlanPanel.tsx` — панель в карточке задачи: шапка со сводкой и кнопками «Сгенерировать план», «Утвердить и запустить» / «Подтвердить изменения плана», «Остановить всё», удаление; баннеры фаз (генерируется, ждёт утверждения, план изменился, ошибка, исход); колонки шагов с узлами (id, заголовок, состояние, зависимости, итерации, стоимость, коммит слияния, причина); карточка конфликта с файлами и тремя действиями человека; пропуск и возврат узла до старта; переход к сессии узла; подвал со стоимостью, бюджетом и интеграционной веткой.
- Панель живёт внутри `TaskDetailModal`, который рендерится через `createPortal` (decision-17); подтверждения остановки и удаления — `dialog.confirm` (DialogHost `z-[10001]`), своих оверлеев панель не создаёт.
- Обновление по событию `plan:event` (подписка на mount, отписка на unmount), без опроса.
- Строки ru/en: секция `plan` в `src/i18n/{types,ru,en}.ts` (состояния узлов, исходы плана, кнопки, баннеры, тексты подтверждений).
- Живой прогон узлов и скриншоты собранного exe — TASK-80.4.
<!-- SECTION:NOTES:END -->
