---
id: TASK-83
title: >-
  Голосовое управление 2.0: push-to-talk, wake word, LLM-разбор команд, диалог с
  озвучкой, голос → компьютер, диктовка
status: Review
assignee: []
created_date: '2026-09-15 03:45'
updated_date: '2026-09-17 12:30'
labels:
  - voice
  - stt
  - tts
  - computer-use
  - ai
  - model-agnostic
milestone: m-0
dependencies:
  - TASK-69
  - TASK-82
priority: medium
type: feature
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Голосовой модуль ProjectHub уже силён: hands-free VAD, локальный Whisper в воркере, 48 интентов в `voiceCommandParser` (навигация, git, процессы, AI-сессии, чтение задач), настраиваемые фразы, выбор устройств, TTS через системный синтез (Piper — TASK-69). Пробелы: команды понимаются только по regex-фразам (нет свободной речи), нет глобального push-to-talk вне окна, нет wake word, ответы агента не озвучиваются (нет диалога), нет голосового управления компьютером и диктовки в чужие окна. Задача закрывает это без привязки к вендору моделей (decision-26 п. 0).

## Что сделать
1. **Push-to-talk и wake word.** Глобальная горячая клавиша через Electron `globalShortcut` (по умолчанию `Ctrl+Shift+Space`, удержание = запись, отпускание = отправка), работает при свёрнутом окне; индикатор в трее (`trayService`, TASK-63). Wake word без внешних библиотек: в hands-free режиме первая фраза чанка сверяется с настраиваемым ключевым словом («Хаб», «Привет, Хаб») по транскрипту Whisper; опционально — Porcupine/openWakeWord как плагин, если понадобится точность (не по умолчанию: openWakeWord — Python).
2. **LLM-разбор свободных команд.** Если `voiceCommandParser` не распознал фразу — она уходит классификатору на **любой настроенной модели** (по умолчанию локальная через Ollama/профиль TASK-70, cheap-тир TASK-79): структурированный ответ `{intent, payload, confidence}` по списку известных интентов + `unknown`; при низкой уверенности — переспросить голосом. Промпт и схема — чистый модуль с тестами; кэш частых фраз.
3. **Диалог с агентом.** Интент `send_prompt` получает продолжение: финальный ответ агента (AI Studio/Swarm) кратко озвучивается через TTS (Piper, длина ответа ограничена, код и диффы не читаются — только сводка), HITL-вопросы агента озвучиваются и принимают голосовой ответ (`agent_approve`/`agent_reject`/`agent_select_option` уже есть). Barge-in: речь пользователя останавливает озвучку (VAD → `stop_reading`).
4. **Голосовое управление компьютером.** Интенты вида «кликни …», «напечатай …», «открой окно …», «прокрути вниз» не парсятся регулярками, а передаются агенту с инструментами `computer_*` (TASK-82) как задача в одном ходе с HITL-политикой computer-use; результат озвучивается («Готово», «Не нашёл кнопку Сохранить»).
5. **Системная диктовка.** Режим «диктовать в активное окно»: распознанный текст печатается через `computer_type` в любое приложение (с подтверждением при первом включении и allowlist окон TASK-82); интеллектуальная пунктуация — через модель (п. 2) при включённой настройке.
6. **Гигиена**: интенты и фразы для новых режимов в `voiceCommandPhrases` (ru/en), i18n, настройки в `VoiceSettingsModal` (вкладка «Диалог и диктовка»), unit-тесты для классификатора/маршрутизации.

## Вне scope
Piper TTS (TASK-69), сам прокси computer-use (TASK-82), обучение wake word.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Глобальный push-to-talk (globalShortcut, настраиваемый) работает при свёрнутом окне с индикацией в трее; wake word по транскрипту включается в настройках
- [ ] #2 Нераспознанная фраза классифицируется настроенной моделью (проверено на локальной модели) в структурированный интент; низкая уверенность — голосовой переспрос; схема и парсер покрыты unit-тестами
- [ ] #3 Финальный ответ агента и HITL-вопросы озвучиваются (сводка, без кода); голосовой ответ на HITL принимается; речь пользователя прерывает озвучку (barge-in)
- [ ] #4 Голосовые команды управления компьютером выполняются агентом через computer_* с HITL-политикой TASK-82 и озвученным результатом; сценарий «открой Блокнот и напечатай …» проходит голосом
- [ ] #5 Режим системной диктовки печатает распознанный текст в активное окно с учётом allowlist; опциональная пунктуация через модель
- [ ] #6 Фразы ru/en, i18n, настройки в VoiceSettingsModal; lint/test зелёные, pack:win собран и проверен голосом
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Что сделано по пунктам плана

**1. Push-to-talk и wake word.**
`electron/services/pushToTalkPolicy.ts` — чистый модуль: валидация accelerator (отвергает чистые модификаторы и F12), санитайзер настроек, конечный автомат удержания. Electron `globalShortcut` не даёт keyup (electron#26301 закрыт), нативных перехватчиков клавиатуры в проекте нет, а `npmRebuild: false` делает новую нативную зависимость дорогой. Удержание восстанавливается по автоповтору `RegisterHotKey`: Chromium не передаёт `MOD_NOREPEAT`, поэтому зажатая клавиша шлёт повторные срабатывания. Дедлайн адаптивный — первый по худшему случаю (1200 мс), дальше наблюдаемый интервал × 2.5. Вне Windows режим `hold` понижается до `toggle` (автоповтора у горячих клавиш там нет).
`electron/services/voiceHotkeyService.ts` — регистрация, таймер, персист `<userData>/voice-hotkey.json`. Побочные эффекты навешивает `main.ts` через handlers, как у `computerUseService`.
Трей: в `TrayState` добавлено `recording`, в `TrayService` — отдельный флаг `setRecording` и `applyIcon()`. Флаг отдельный намеренно: `notificationService.refreshTray()` дёргает `setState` на каждом событии шины и затёр бы индикацию. Приоритет `attention` > `recording` > `working` > `idle`. Четвёртая иконка сгенерирована `scripts/gen-tray-icons.mjs`.
`main.ts`: инициализация в `whenReady`, `shutdown()` в `performGracefulShutdown`, плюс страховочный `app.on('will-quit')` → `globalShortcut.unregisterAll()` (его в проекте не было вовсе).
Рендерер: `voiceService.beginPushToTalk/endPushToTalk`, отдельная ветка в `processAudioChunkVAD` — при удержании пишется всё подряд, детектор тишины фразу не режет; ограничение 60 с от «залипшей» клавиши.
Wake word: `src/services/wakeWord.ts` — сверка по словам (а не по подстроке: «хабар» не обращение), регистр и «ё», окно 8 с на команду отдельной фразой.

**2. LLM-классификатор.**
`electron/services/voiceCommandClassifier.ts` — каталог интентов как данные, промпт, толерантный парсер, LRU-кэш (64 записи, TTL 24 ч). JSON-режима нет ни у одного провайдера проекта, у Anthropic его нет вовсе, tool-use не умеет часть локальных моделей — поэтому контракт: один JSON-объект, извлекаемый `extractJsonObject`, плюс запасной формат «голый идентификатор интента». Номера модель даёт с единицы, наружу уходит индекс с нуля. Порог уверенности 0.6 не покидает main: в рендерер уходит флаг `confident`.
`aiAgentService.complete()` — одноразовый вызов без инструментов, без `buildSystemPrompt` (там вшита преамбула «You are Claude Code»), `temperature: 0`, свой таймаут. Через `claudeBridgeService` идти было нельзя: при `provider: anthropic` без ключа он запускает процесс Claude CLI. Вендорского дефолта модели нет — при пустой модели честная ошибка (decision-26 п. 0).
`electron/services/llmEndpoint.ts` — разрешение адреса OpenAI-совместимых провайдеров вынесено из приватного метода и покрыто тестами.
Порядок разбора: ключевое слово → регулярки → модель. Привычные команды остаются мгновенными и бесплатными.

**3. Диалог с озвучкой.**
`src/services/ttsSummary.ts` — снятие кода, диффов, таблиц и разметки, обрезка по границе предложения. `useAIStudioStore` в `onAIComplete` шлёт событие `projecthub:agent-answer`; решение, читать ли вслух, принимает голосовой модуль.
Вопросы агента озвучиваются подпиской на `useHitlStore`. Голосовой ответ на HITL теперь покрывает **общую** очередь, а не только `pendingApprovals` AI Studio: запросы внешних сессий и `computer_action` от MCP-моста туда не попадают и голосом раньше были недоступны.
Barge-in: `src/services/bargeInDetector.ts` работает **внутри** существующего гейта `ttsMuted`, второго механизма глушения не заводилось. Нужны и громкость (порог 0.08 против 0.012 у обычного VAD), и длительность 350 мс — случайный щелчок чтение не прервёт.

**4. Голос → компьютер.**
IPC `voice:runComputerTask` — один ход агента, где `allowedToolNames` ограничен именами `computer_*`, поэтому родные инструменты (чтение файлов, запуск команд) в голосовую команду не попадают. HITL, allowlist, аудит и kill-switch остаются внутри `computerUseService.callTool` — не дублируются и не обходятся. Есть собственный таймаут: `streamChat` при отмене не зовёт ни `onComplete`, ни `onError`, и промис завис бы.

**5. Системная диктовка.**
IPC `voice:dictateText` → `computer_type`; опциональная пунктуация через `complete()`, при недоступности модели печатается исходный текст. В рендерере режим включается с подтверждением при первом включении (`dictationConfirmed`), выход — фразой «конец диктовки». Ключевое слово в режиме диктовки намеренно не применяется: диктуют прозу.

**6. Гигиена.**
`dictation_start`/`dictation_stop` в `voiceCommandPhrases` (новая категория `dialog`, фразы ru/en), i18n ru/en для всех новых строк, вкладка настроек «Диалог и диктовка» в существующей модалке (`createPortal`, `z-[9999]` — правило 19).

**ADR:** decision-30 (активация: автоповтор globalShortcut и wake word по транскрипту, с отвергнутыми альтернативами — uiohook-napi, приватное API рантайма computer-use, Porcupine/openWakeWord), decision-31 (схема ответа классификатора и одноразовый вызов модели).

## Проверки

lint — 0 ошибок, 503 предупреждения (ровно базовый уровень, техдолг не вырос); `vitest` — 86 файлов / 906 тестов (было 80 / 834); `tsc` — чисто; `check-bundle` — чисто (main.js 652 КБ, тяжёлые зависимости внешние); `lint:docs` и `index-docs` — чисто, оба ADR в индексе; `pack:win` — собран.

Живая проверка собранного `release/win-unpacked/ProjectHub.exe` (Playwright `_electron` поверх exe, два запуска):
- в `main.log` дважды `[VoiceHotkey] Push-to-talk на Control+Shift+Space (режим: hold)` — глобальная клавиша регистрируется в упакованном приложении;
- вкладка «Диалог и диктовка» открывается и отрисована целиком: поле сочетания со значением `Control+Shift+Space` и зелёной отметкой «Registered with the system», режим «Hold the key (records while held)», тумблер индикации в трее, блок wake word с фразами-чипами (выключен по умолчанию), тумблеры озвучки ответов и вопросов, перебивания голосом, блок диктовки с пунктуацией;
- вкладки «Speech (TTS)» и «Command Phrases & Aliases» рендерятся; счётчик настраиваемых команд вырос с 21 до 23 — обе команды диктовки зарегистрированы, категория «Диалог и диктовка» есть в фильтре;
- ошибок консоли рендерера и строк `[Renderer] … error` в `main.log` нет.

`voice-hotkey.json` при старте не пишется — настройки сохраняются только при изменении, как и `computer-use.json`.

## Что осталось непроверенным вживую

Всё, что требует слуха и микрофона: фактическая запись по удержанию клавиши, распознавание wake word, срабатывание barge-in, скачивание голоса Piper кнопкой с прогрессом (~64 МБ, голоса не скачаны), вывод в выбранное устройство (`setSinkId`), отсутствие реакции VAD на собственную речь, команды `read_tasks`/`read_doc`/`stop_reading` с загруженным голосом.

Пункты 4–5 проверены только по ветке отказа: `computerUse` на машине выключен (`computer-use.json` отсутствует), и включать его самостоятельно нельзя — правило 20 infra-dev и decision-27 п. 6.

Классификатор не проверен на живой модели: в `~/.projecthub/ai-config.json` провайдер `anthropic` без API-ключа, а этот путь намеренно не ходит через Claude CLI. На такой конфигурации `complete()` возвращает понятную ошибку, и рендерер показывает «Модель для разбора команд недоступна».

Дефекты TTS, найденные при разборе кода TASK-69, вынесены в TASK-87 и здесь не чинились.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-09-16 09:01
---
Найдено при проверке владельцем (2026-09-16): push-to-talk не срабатывал — «открой задачи» не выполнялось.

Причина — гонка в `voiceService.beginPushToTalk()`. Метод ждал `startHandsFreeListening()`, а холодный старт занимает секунды (getUserMedia, AudioContext, воркер Whisper — 2381 мс по логу). Отпускание клавиши приходило раньше: `endPushToTalk()` видел `pushToTalkActive === false`, выходил вхолостую, после чего запуск всё-таки выставлял флаг — и режим записи залипал включённым навсегда. Первое нажатие гарантированно терялось.

Исправление: режим включается сразу при нажатии, у нажатия появилось «поколение» (`pushToTalkSeq`). Если клавишу отпустили во время старта, запуск отменяется и захват, открытый ради этого нажатия, закрывается. Залипание исключено.

По запросу владельца в ту же задачу добавлен крупный HUD распознанной речи (как в Talon Voice): системный оверлей переехал вниз по центру экрана и получил три режима — полоса 760×96, широкая полоса 1100×190 на время записи по горячей клавише (текст 34px) и окно 86%×60% экрана в режиме диктовки (текст 52px). Показывается распознанная фраза и итог команды. Окно не перехватывает фокус, поэтому в диктовке не мешает целевому приложению. Геометрию применяет main по полю `mode` в `voice:overlay-sync`.

Проверки после правок: tsc чисто, lint 0 ошибок / 503 предупреждения (базовый уровень), 86 файлов / 906 тестов, check-bundle чисто, pack:win пересобран (exe заменён, подтверждено временем модификации).

Отдельно выяснилось, почему трейс голосового конвейера не виден в логах упакованной сборки: `console.log` рендерера уходит в `logger.debug`, а минимальный уровень при `app.isPackaged` — `info`. Для диагностики нужен запуск с `PROJECTHUB_LOG_LEVEL=debug`.

Живой перетест push-to-talk владельцем ещё не пройден.

Долг: защита от гонки живёт внутри `voiceService` и не покрыта unit-тестами — логика завязана на асинхронный старт захвата. Стоит вынести счётчик поколений в чистый модуль и закрыть тестом (правило 17).
---

created: 2026-09-16 10:29
---
Решение владельца (2026-09-16): работу над push-to-talk остановить, оставить как есть на Ctrl+Shift+Space.

Причина — архитектурная, а не техническая: push-to-talk требует клавиатуры и потому противоречит целевому сценарию «полностью без клавиатуры и мышки». Сам пункт пришёл из doc-6 (раздел «Режимы активации») и был записан в план этой задачи до начала работ; при живой проверке выяснилось, что в целевой сценарий он не вписывается.

Основной путь для работы без клавиатуры — wake word по транскрипту, он реализован в этой же задаче и от горячей клавиши не зависит: включается на вкладке «Диалог и диктовка», дальше «Хаб, открой задачи».

Известный незакрытый пробел: отпускание клавиши не детектируется — критерий приёмки #1 в части push-to-talk не выполняется. Вынесено в TASK-89 с низким приоритетом, там же описано, как измерить автоповтор и что делать, если его нет. Остальная часть критерия #1 (wake word включается в настройках, индикация в трее, регистрация клавиши при свёрнутом окне) подтверждена.

Нативный перехватчик клавиатуры рассмотрен и отклонён. Уточнение к [[decision-30]]: аргумент «`npmRebuild: false` делает нативную зависимость дорогой» оказался слабее, чем был подан: `uiohook-napi` — модуль N-API, а N-API стабилен по ABI и для Node, и для Electron, то есть пересборка ему не нужна, и готовые сборки под win32-x64 у него есть. Отказ стоит на другом основании: тратить нативную зависимость на клавишу в сценарии «без клавиатуры» нелогично. В проект зависимость не добавлялась.

Правый Ctrl отдельно назначить нельзя в принципе: акселераторы Electron не различают левый и правый модификатор, а чистый модификатор акселератором не является. Попутно валидатор accelerator ослаблен: теперь допускаются одиночные клавиши, которыми не набирают текст (CapsLock, ScrollLock, Pause, Insert, F1–F24, кроме F12), но не буквы и цифры — глобально перехваченная буква сделала бы её ввод невозможным во всех программах. Дефолт остался Ctrl+Shift+Space.
---

created: 2026-09-16 11:57
---
Живая проверка в GUI (2026-09-16). Задача остаётся в Review: основная её часть на этой машине непроверяема.

**Ключевой факт: на машине нет устройства аудиозахвата.** Проверено двумя способами — в работающем приложении `navigator.mediaDevices.enumerateDevices()` возвращает `audioinput: []`, а на уровне ОС единственная аудиоконечная точка `Remote Audio` (сеанс RDP), устройств захвата нет. Поэтому push-to-talk, wake word, barge-in, диктовка и голосовые команды не «не проверены», а проверить их здесь нечем.

**Что подтверждено живьём:**

- **Озвучка через Piper работает** — общая основа пункта 3 (диалог с озвучкой). Голос `ru_RU-dmitri-medium` установлен кнопкой и синтезирует: первый чанк 134 мс на прогретой модели, RTF ≈ 0,06, вывод через выбранное устройство (`setSinkId`) без ошибок. Подробности — в комментарии к TASK-69.
- **Остановка озвучки** прекращает генерацию (после неё чанков нет, очередь в main пуста) — механизм, на котором стоит barge-in.
- **Глобальная горячая клавиша регистрируется** в упакованном приложении: `[VoiceHotkey] Push-to-talk на Control+Shift+Space (режим: hold)` в `main.log` при каждом старте.
- **Вкладка «Диалог и диктовка»** открывается и отрисована целиком (подтверждено ранее и в этом прогоне).

**Что проверить невозможно на этой машине (вынесено в TASK-90):**

- AC#1 — фактическая запись по удержанию клавиши и wake word (нужен микрофон; отдельно помним про TASK-89: отпускание клавиши не детектируется).
- AC#3 — barge-in: пороги `bargeInDetector` (громкость 0,08 и 350 мс) проверяются только живым голосом.
- AC#5 — диктовка в активное окно.
- AC#4 — голос → компьютер: дополнительно требует включённой фичи `computerUse`, которую агент не включает самостоятельно (правило 20 infra-dev, decision-27).
- AC#2 — LLM-разбор свободных команд: блокируется независимо от микрофона, в `~/.projecthub/ai-config.json` провайдер `anthropic` без API-ключа, а этот путь намеренно не ходит через Claude CLI. Нужна любая настроенная модель.

Итог: из шести критериев приёмки живой проверкой закрыта только инфраструктурная часть (регистрация клавиши, интерфейс, работающая озвучка как основа диалога). Закрывать задачу по результатам этого прогона нельзя.
---

created: 2026-09-17 11:21
---
Промежуточный итог живой проверки 2026-09-17 (фейковый микрофон Chromium + речь Piper, упакованный exe).

**Подтверждено живьём:** AC#1 wake word — фраза без обращения игнорируется (вкладка не меняется), «Привет, хаб! Покажи задачи!» переключает Git → Задачи. AC#3 barge-in — работает после исправления (см. ниже): «Замолчи…» обычной громкости и короткое «Хватит.» прерывают чтение за ~0.4–0.7 с.

**Найдено и исправлено (не закоммичено):**
1. **Barge-in не срабатывал никогда**: требовалось 350 мс подряд чанков громче 0.08, а RMS речи в 85-мс чанках 0.02–0.3 с провалами между словами. Новый детектор: серия речи с паузами до 300 мс, ≥2 громких чанка, порог относительный — 0.3 от громкости фраз самого пользователя (p75 звучащих чанков), запасной 0.08. Тесты на реальных рядах RMS из приложения.
2. Крах приложения при озвучке после распознавания (onnxruntime 1.21 vs 1.28) — затрагивал и озвучку ответов/переспрос этой задачи; исправлен в рамках TASK-69 (Piper в utilityProcess).

**Наблюдения, будут отдельными задачами:** whisper-base часто слышит «хаб» как «хап/кап» — wake word срабатывает нестабильно; после barge-in начало фразы уходит на детекцию и не попадает в распознавание.

**Осталось проверить:** AC#1 индикация в трее; AC#2 классификатор на qwen2.5 (Ollama, конфиг переключается временно); AC#3 озвучка ответа агента и HITL + голосовой ответ; AC#4 «открой Блокнот и напечатай …» (computerUse включается на время прогона по разрешению владельца); AC#5 диктовка; AC#6 финальные проверки.
---

created: 2026-09-17 12:30
---
## Передача дел в новую сессию (2026-09-17, часть 1/2: решения и состояние)

Владелец разрешил **агенту самому отмечать чек-листы** TASK-69 и TASK-83 (в обход decision-28), затем коммит и закрытие обеих задач. Коммит — только по явной просьбе владельца.

### Решения владельца в этой сессии
- Голосовые критерии закрываются прогоном с **фейковым микрофоном Chromium** (`--use-fake-device-for-media-stream --use-file-for-fake-audio-capture=<wav>`, речь синтезирует Piper).
- Импорт голоса: **проба модели в отдельном процессе** (`utilityProcess`).
- Конфликт onnxruntime (Whisper 1.21 vs sherpa 1.28): **весь Piper в `utilityProcess`**.
- Разбор свободных команд — **через Claude Code по подписке** (`claude -p`, ~3 с), не Ollama; AI Studio остаётся `anthropic/sonnet` без ключа.
- Прогон управления компьютером — пока владелец не трогает мышь (~3 мин), с предупреждением перед стартом.
- Решение агента (сообщить владельцу в итоге): голосовой переспрос на `unknown` только при включённом wake word.

### Незакоммиченные изменения (lint 0 ошибок/502 warn, 94 файла/1029 тестов, pack:win 20:24)
- **TASK-69:** `ttsWorker.mjs` → utilityProcess, `piperTtsService` на `utilityProcess.fork`; проба импорта `ttsProbe.mjs` + `ttsVoiceProbe.ts` + чистый `ttsVoiceProbeOutcome.ts`; коды `model_rejected/probe_timeout/probe_unavailable` + i18n; `isNativeModuleMissingError`; `ttsStatusView.ts` + обновление статуса после Preview/по ошибке; парсер: `read_tasks`/`read_doc` перед навигацией и формы «прочитаю задачу»/«прочитая документ».
- **TASK-83:** `bargeInDetector` — серия речи с паузами ≤300 мс, ≥2 громких чанка, порог 0.3 × p75 громкости фраз пользователя (запасной 0.08); `wakeWord` — встроенные ошибки Whisper «хап»/«хабп», «кап» только после приветствия; `claudeCliCompletion.ts` + `complete()` для `anthropic` без ключа через `claude -p`; `voiceClassifiedCommand.ts` — вкладка в payload для `navigate_*` (классифицированная навигация не работала); `toggle/hide/show_sidebar` в ветке navigation виджета (фразы меню из настроек не работали вообще); голосовой переспрос на `unknown`; `voice:runComputerTask` для `anthropic` без ключа → `claudeBridgeService.runAgentTask` с `builtinTools: []` (`--tools=`) и `systemPromptAddon`; парсер: `run_tests`/`run_deploy` только короткой командой.

### Подтверждено живьём
- TASK-69: AC#1, #4, #5, #6, #7 (см. комментарий #2 TASK-69); AC#2/#3 ранее; #8/#9 сборка и тесты.
- TASK-83: AC#1 wake word (в т.ч. «Привет, хап…»); AC#2 через подписку: «хочу посмотреть, что сейчас в работе» → Git→Задачи, «сделай что-нибудь хорошее» → голосовой переспрос; AC#3: ответ агента озвучен, HITL `Write тест.txt` озвучен, голосовое «Отклонить» → `deny/local` в audit, barge-in «замолчи»/«хватит»; AC#5 частично: режим диктовки включается/выключается голосом, текст ушёл в `computer_type`, политика отклонила (окно вне allowlist).
- AC#4: классификатор → `computer_task` → CLI-агент по подписке → `computer_open_application notepad.exe` дошёл до политики; помешали kill-switch (владелец двигал мышь) и дефект режима `onlyAllowlistedWindows` (см. часть 2).
---

created: 2026-09-17 12:30
---
## Передача дел в новую сессию (часть 2/2: что осталось и как проверять)

### Осталось сделать
1. **Повторить AC#4/AC#5** (владелец не трогает мышь ~3 мин — предупредить и дождаться «готов»): включить computerUse, allowlist `notepad.exe`, `outsideAllowlist: 'deny'`, **`onlyAllowlistedWindows: false`** (в режиме true запуск приложения из allowlist запрещён: «цель не определена»). WAV `s12.wav`. После — закрыть Блокнот без сохранения (`Stop-Process`), **выключить computerUse** (сейчас `computer-use.json` enabled=false, allowlist notepad сохранён).
2. AC#1: индикация в трее при нажатии push-to-talk (или `main.log` debug `[VoiceHotkey]`) — в объёме, суженном владельцем (TASK-89).
3. ADR: **decision-34** — Piper в `utilityProcess` + проба импорта (в decision-25 отметить, что worker_threads НЕ изолирует нативные падения, ссылка); **decision-35** — служебные запросы и голос→компьютер через Claude Code по подписке (пересмотр decision-31 про CLI; замеры: 8.2 с по умолчанию, ~2.8 с sonnet с лёгкими флагами, ~9 с haiku). Номера уже упомянуты в коде. Затем `npm run index-docs`.
4. Завести задачи: (а) импорт `.onnx` с именем встроенного голоса выдаёт себя за встроенный и при неудачной пробе **удаляет рабочий встроенный голос**; (б) `onlyAllowlistedWindows` запрещает `open_application` для приложения из allowlist; (в) после barge-in голова фразы не попадает в распознавание; (г) `send_prompt`: «Напиши агенту, …» с запятой обрезается до «агенту, …»; (д) обновить TASK-90: остаются только эхо озвучки через колонки (AEC) и живой push-to-talk.
5. Восстановить состояние владельца: localStorage голоса из `voice-config-backup.json` (менялись `language`→ru, `ttsEngine`→piper, `ttsVoiceId`, `wakeWordEnabled`, `speakAgentAnswers`, `speakHitlQuestions`, `dictationConfirmed`, `llmFallbackEnabled`), язык интерфейса обратно EN. `ai-config.json` уже восстановлен.
6. Финальные проверки: lint, test, check-bundle, lint:docs, index-docs, pack:win; отметить все AC TASK-69/TASK-83 с пометками о сужениях (push-to-talk — TASK-89; эхо через колонки — TASK-90); notes/final summary; предложить коммит.

### Тестовая обвязка
Каталог `C:/Temp/claude/f--ProjectHub/c73315cd-d6b3-417b-9a79-4f46b3e2afd9/scratchpad/voice-e2e/`: `synth-seq.mjs` (WAV-сценарии 48 кГц, голос через env `VOICE`, по умолчанию Ирина), `whisper-check.mjs` (офлайн-распознавание той же моделью), `cdp.mjs`/`ev.mjs`/`scenario.mjs` (CDP-драйвер: таймлайн консоли, TTS-событий, вкладок, `--patch`, `--watch`, `--at`), `pick-file.ps1` (нативный диалог через WM_SETTEXT), бэкапы `voice-config-backup.json`, `ai-config.backup.json`.
Запуск: env-tools `projecthub-voice-e2e`, `cmd /c "set ELECTRON_RUN_AS_NODE=&& set PROJECTHUB_LOG_LEVEL=debug&& release/win-unpacked/ProjectHub.exe --remote-debugging-port=9333 --use-fake-device-for-media-stream --use-file-for-fake-audio-capture=<wav>"`. **Порт 9222 занят мёртвым сокетом** — использовать 9333 и `CDP_PORT=9333`.
Ловушки: bash съедает обратные слэши (пути в JS — через `String.fromCharCode(92)`, регулярки править только Edit); перед сборкой убивать ProjectHub.exe и процессы `computer-use-mcp`, иначе EPERM и старый exe (сверять время exe); whisper-base путает отдельные фразы — подбирать формулировку офлайн (надёжно: «Какие задачи?», «Прочитай документ.», «Хватит.», «Замолчи.», голос Ирина).
---
<!-- COMMENTS:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Голосовое управление 2.0: глобальный push-to-talk, wake word по транскрипту, разбор свободных команд любой настроенной моделью, озвучка диалога с barge-in, голосовые команды компьютеру и системная диктовка.

**Новые чистые модули с тестами:** `pushToTalkPolicy` (детект удержания по автоповтору globalShortcut), `wakeWord` (сверка обращения по словам), `voiceCommandClassifier` (каталог интентов, промпт, толерантный парсер, LRU-кэш), `llmEndpoint` (адреса OpenAI-совместимых провайдеров), `bargeInDetector` (перебивание озвучки), `ttsSummary` (подготовка ответа агента к чтению вслух). Шесть новых тестовых файлов, +72 теста.

**Ключевые решения** зафиксированы в decision-30 и decision-31. Главные из них: удержание клавиши восстанавливается по автоповтору `RegisterHotKey` без единой новой зависимости (нативный хук отвергнут из-за `npmRebuild: false`); wake word считается по готовому транскрипту Whisper, без Porcupine и openWakeWord; контракт с моделью — один JSON-объект плюс запасной «голый идентификатор», без `response_format` и tool-use, которых нет у части провайдеров и локальных моделей.

**Вендор-нейтральность:** добавлен `aiAgentService.complete()` — одноразовый вызов без инструментов и без вендорской преамбулы, не ходящий через Claude CLI и не подставляющий вендорских дефолтов модели (decision-26 п. 0).

**Безопасность не ослаблена:** голос → компьютер и диктовка работают исключительно через `computerUseService.callTool`, где остаются HITL, allowlist, аудит и kill-switch; голосовой ход агента ограничен только инструментами `computer_*`; диктовка требует явного подтверждения при первом включении. Фичу `computerUse` задача не включает и включать не предлагает.

**Побочное улучшение:** голосовые «одобрить»/«отклонить»/«вариант N» теперь работают и для общей очереди HITL, а не только для карточек AI Studio, — запросы внешних сессий и `computer_action` от MCP-моста раньше голосом были недоступны.

Проверки: lint 0 ошибок (503 предупреждения — базовый уровень), 86 файлов / 906 тестов, `tsc` и `check-bundle` чисто, `pack:win` собран, регистрация глобальной клавиши подтверждена в упакованном приложении по `main.log`.

Непроверенным вживую осталось всё, что требует слуха и кликов: озвучка с голосами Piper (не скачаны), `setSinkId`, реакция VAD на собственную речь, wake word и barge-in, а также «счастливый путь» пунктов 4–5 — управление компьютером на машине выключено.
<!-- SECTION:FINAL_SUMMARY:END -->
